# 🧠 让国内朋友能打开 weilinhu.com — 全流程学习笔记

> 2026-04-24 折腾一天记下来的，写给六岁版本的自己。

---

## 一、这网站到底是个啥？

`weilinhu.com/smash.html` 是一个 AI 街机小游戏，6 个角色专门"骂醒打工人"。

它由 **4 个积木**拼起来：

```
  客户浏览器
      │
      ▼
┌─────────────────┐
│ ① 域名          │  weilinhu.com（你在 Namecheap 买的）
│    = 你家门牌号  │
└────────┬────────┘
         │ 谁帮你指路？
         ▼
┌─────────────────┐
│ ② 邮局（NS）    │  以前：Namecheap 邮局
│    = 谁收发信   │  现在：Cloudflare 邮局（更多网点）
└────────┬────────┘
         │ 把客人送到店铺
         ▼
┌─────────────────┐     ┌─────────────────┐
│ ③ 店铺          │     │ ④ 服务员 Worker │
│  GitHub Pages   │ ──▶│ smash-proxy      │──▶ Claude API
│  = HTML 文件家  │     │ = 帮你跑腿的人   │   = 真的 AI
└─────────────────┘     └─────────────────┘
```

---

## 二、为什么国内打不开？

两个拦路虎：

| 拦路虎 | 原因 |
|---|---|
| 🚧 `github.io` 被墙 | GitHub Pages 服务器 IP 在国内经常被污染 |
| 🚧 `workers.dev` 被墙 | Cloudflare 的 Worker 默认域名 `*.workers.dev` 国内**完全打不开** |

即使用自定义域名 `weilinhu.com` 挂 GitHub Pages，如果 DNS 还在 Namecheap，国内解析也不稳。

---

## 三、解决方案（今天做的事）

### 🔧 改 1：搬家到 Cloudflare DNS

**Nameserver（NS）是什么**：
告诉全世界"要查 weilinhu.com 的 IP，应该问哪台服务器"。
你换 NS = 告诉全世界"以后问 Cloudflare，别问 Namecheap 了"。

**为什么 Cloudflare 比 Namecheap 好**：
- 全球 CDN 节点多（300+），国内虽然没直接点位，但路由比 Namecheap 快
- 橙云代理：客户不直接连 GitHub Pages（IP 可能被墙），而是连 Cloudflare → Cloudflare 再连 GitHub。相当于有个"中转站"
- 免费

**具体操作（30 分钟，5 步）**：
1. Cloudflare 注册 + 添加 weilinhu.com
2. Cloudflare 自动扫你当前 DNS 记录（4 条 A + CNAME + MX + TXT）
3. 拿到 Cloudflare 给的 2 个 NS：`betty.ns.cloudflare.com` / `yadiel.ns.cloudflare.com`
4. Namecheap 把 NS 改成上面这俩
5. 5 分钟后 Cloudflare 发邮件"active 了"

**⚠️ 注意事项**：
- 改 NS 之前，必须先关 DNSSEC（在 Namecheap 的 Advanced DNS 里）。开着会把域名烧死几小时
- MX 邮件记录必须保持 DNS only（灰云），不能代理
- A 和 CNAME 用橙云（代理），这是国内访问的关键

### 🔧 改 2：给 Worker 挂自有子域名

**问题**：Worker 默认地址 `smash-proxy.weilin-bellina-hu.workers.dev` 在国内打不开。

**方案**：把 Worker 挂到 `smash-api.weilinhu.com` 这个自有子域——因为你的 weilinhu.com 已经在 Cloudflare 代理，子域也自动走 Cloudflare。

**操作**：
1. Cloudflare → Workers & Pages → smash-proxy
2. Settings → Domains & Routes → + Add → Custom Domain
3. 填 `smash-api.weilinhu.com` → Add
4. Cloudflare 自动建 CNAME + SSL 证书（1 分钟）

**然后改代码**：
- `smash.html` 和 `smash-fr.html` 里的 `PROXY_URL`
- 从 `https://smash-proxy.weilin-bellina-hu.workers.dev/scold`
- 改成 `https://smash-api.weilinhu.com/scold`

### 🔧 改 3：SSL/TLS 模式必须是 Full（或 Full strict）

**为什么**：Cloudflare 和 GitHub Pages 中间这段要 HTTPS，不能降级成 HTTP。
- Flexible = 客户 HTTPS → CF → 明文 HTTP → GitHub。GitHub 不收明文，会死循环
- Full = 全程 HTTPS ✓

**位置**：Cloudflare → 左侧 SSL/TLS → Overview → Encryption mode

---

## 四、流程总结

```
① Namecheap 改 NS（自己点）
       ↓ 5-30 分钟传播
② 收到 Cloudflare "active" 邮件
       ↓
③ Cloudflare SSL 设 Full（自己点）
       ↓
④ Worker 加 Custom Domain smash-api.weilinhu.com（自己点）
       ↓
⑤ 代码里改 PROXY_URL（Claude 做 + push）
       ↓
⑥ 国内浏览器打开 weilinhu.com/smash.html 验证
```

---

## 五、费用

| 项 | 费用 | 备注 |
|---|---|---|
| Cloudflare Free Plan | **$0** | 永久免费，每天 10 万 Worker 请求配额 |
| Cloudflare Worker | $0 | 免费额度内 |
| 子域 smash-api.weilinhu.com | $0 | 子域免费，挂在你已有域名下 |
| GitHub Pages | $0 | 一直免费 |
| Namecheap 域名续费 | ¥$10/年 | 跟 Cloudflare 无关 |

**总额外成本：0 元**。

---

## 六、如果将来国内还是访问不稳

Cloudflare 免费版**不保证国内 100% 通**。真要完全稳：

| 方案 | 成本 | 周期 |
|---|---|---|
| 腾讯云香港轻量云 | ¥24/月 | 当天上线，不用备案 |
| 阿里云国内 ECS + ICP 备案 | ¥50/月 + 20 天备案 | 最稳但最麻烦 |

暂时不用做——先看 Cloudflare 免费版效果如何。

---

## 七、以后忘了怎么办

**再次换 Worker API**：
- Cloudflare → Workers & Pages → 选 Worker → Settings → Domains & Routes → + Add

**再次换 DNS**：
- 别乱换。如果真要换回 Namecheap，Namecheap → Domain → NAMESERVERS → 改回 `Namecheap BasicDNS`，然后记得手动把 Cloudflare 里原有的 A / CNAME / MX 记录都**重新在 Namecheap 里创建一遍**（不然邮件会挂）

**Worker 代码在哪**：
- 不在 portfolio git 仓库里。在 `~/Desktop/Editions/smash-proxy/` 或者 Cloudflare Dashboard 里直接编辑

**PROXY_URL 在代码里的位置**：
- `smash.html` 和 `smash-fr.html` 顶部有一行 `const PROXY_URL = '...'`

---

## 八、今天顺手做了的（加分项）

除了国内访问，一路上还做了：

| 改动 | 位置 |
|---|---|
| 删掉三档难度，锁"抽醒" | smash.html / smash-fr.html |
| FIGHT 前加 3→2→1 倒数 | showFight() |
| CRT 底噪 ambient bed | smash-engine.js |
| 6 个 BOSS 专属出场 stinger | smash.html / smash-fr.html |
| 打字机 blip 按 BOSS 变音色 | SFX.type() |
| KO 开场 400ms 静默 punch | SFX.ko() |
| Hover tick + 输入框通电音 | SFX.tick() / SFX.powerOn() |

---

**记住**：你不需要记住技术细节，只需要记住**流程顺序**（第四节那个流程图）。
真搞不懂再问 Claude，他会跟你再讲一遍。
